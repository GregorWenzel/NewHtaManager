<UserControl x:Class="HtaManager.GUI.StudyEditor.ModelSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HtaManager.GUI.StudyEditor"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Button Grid.Row="0" Content="Interventionelle Studie" Command="{Binding SetInterventionalStudyCommand}" Width="150"
                Margin="0,0,0,5" Padding="5" HorizontalAlignment="Center" VerticalContentAlignment="Center" Height="30"/>
        <Button Grid.Row="1" Content="Beobachtungsstudie" Command="{Binding SetObservationalStudyCommand}" Width="150"
                Margin="0,5,0,0" Padding="5" HorizontalAlignment="Center" VerticalContentAlignment="Center" Height="30"/>
    </Grid>
</UserControl>
